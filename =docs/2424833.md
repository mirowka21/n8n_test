=1. **Фильтр страниц:**
   - Исправить параметр `Value=~bqrt6Z4Er0Kr` на корректный CQL-запрос (например, `space=KEY`).

2. **Обработка ошибок:**
   - Добавить уведомления при сбоях (Email, Slack).

3. **Модель LLM:**
   - Рассмотреть более производительные модели Ollama (например, <code>llama3.2</code>) для лучшего качества преобразования.

4. **Инкрементальная синхронизация:**
   - Использовать `version` страницы Confluence для обновления файлов только при изменении содержимого.

5. **React Official Website (другой ссылка):**
   - Было очень коротким или модель не получила полного контента.